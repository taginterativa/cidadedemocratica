<% if tags and (tags.size > 0) -%>
<% if params[:tag_id] -%>
<p class="cloud_titulo">
  Temas relacionados
</p>
<% end -%>
<div class="cloud rounded">
<% tag_cloud(tags, %w(nuvem_1 nuvem_2 nuvem_3 nuvem_4 nuvem_5)) do |tag, c| -%>
  <span class="<%= c %>"><%= link_to_with_context tag.name, :tag_id => tag.id, :page => nil %></span>
<% end -%>
</div>
<% else -%>
<div class="cloud rounded">
  <p class="no_tags">
    Ainda não há temas (ou tags...) cadastradas.<br />
    <span class="call2action">Indique uma questão e associe-a aos temas apropriados!</span><br />
    <span class="hint">Use os botões no topo à direita:
    <br/>
    “Aponte um problema” e “Crie uma proposta”.</span>
  </p>
</div>
<% end -%>